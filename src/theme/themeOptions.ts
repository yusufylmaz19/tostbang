import { darkPalette, lightPalette } from "./colors";

const themesOptions = {
    ["light"]: {
        palette: lightPalette,
    },
    ["dark"]: {
        palette: darkPalette,
    },
};

export default themesOptions;
